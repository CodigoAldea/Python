# they are ordered
# they can contain arbitrary objects
# Index / slicing
'''a=[1,2,3,4,5,6]
# [:]
s = 'python'
print(s[:])
print(a[:]) # copy of our list'''
# in / Not in 
# concatination (+) and replication (*)
# len(), min() and max()
# they can be nested
# Mutable
#functions 
    # append : a.append(<object>)
    # extend : a.extend(<iterable>)
    # insert : a.insert(<index>,<object>)
    # remove : a.remove(<obj>)
    # pop : a.pop(index =-1)
# Dynamic
# looping in a list.
a=[1,2,3,4,5,6,7]
for i in range(len(a)):
    print(a[i])